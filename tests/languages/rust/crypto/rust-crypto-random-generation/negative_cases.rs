fn non_crypto_random() {
    use std::collections::HashMap;
    let mut map = HashMap::new();
    map.insert("key", "value");
}

fn string_operations() {
    let s = "random string";
    let len = s.len();
}

fn math_operations() {
    let x = 42;
    let y = x * 2;
}
